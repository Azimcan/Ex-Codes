#include <stdio.h>
#include <stdlib.h>

struct n {
	int data;
	struct n* left;
	struct n* right;
};
typdef struct n node;

int listele(node* tree);

int main() {
	node* tree = (node*)malloc(sizeof(node));
	tree->next = 

	return 0;
}

int listele
